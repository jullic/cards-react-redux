
const Cards = () => {


    return (
        <div className="cards__cards">
            {/* {filterCards.map(card => <CardsCard key={card.id} card={card} onDelete={() => onDelete(card.id)}/>)} */}
        </div>
    )
};

export default Cards;